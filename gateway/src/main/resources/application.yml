server:
  port: 8080

spring:
  application:
    name: gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


logging:
  level:
    root: INFO
    com.microservices.gateway: INFO
    org.springframework.cloud.gateway: INFO

jwt:
  secret: your-super-secret-key-must-be-32-chars-minimum